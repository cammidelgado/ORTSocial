@model IEnumerable<ORTSocial.Models.Plan>

@{
    ViewData["Title"] = "Index";
}

<h1>Planes</h1>

<p>
    @*  <a asp-action="Create">Create New</a>*@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Costo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantFamiliares)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cartilla)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Costo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantFamiliares)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cartilla.Nombre)
            </td>
            <td>
               @* <a asp-action="Edit" asp-route-id="@item.IdPlan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPlan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPlan">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
