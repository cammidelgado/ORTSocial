@model ORTSocial.Models.CartillaMedico

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CartillaMedico</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="idCartillaMedico" />
            <div class="form-group">
                <label asp-for="IdCartilla" class="control-label"></label>
                <select asp-for="IdCartilla" class="form-control" asp-items="ViewBag.IdCartilla"></select>
                <span asp-validation-for="IdCartilla" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdMedico" class="control-label"></label>
                <select asp-for="IdMedico" class="form-control" asp-items="ViewBag.IdMedico"></select>
                <span asp-validation-for="IdMedico" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
