@model IEnumerable<ORTSocial.Models.CartillaMedico>

@{
    ViewData["Title"] = "Index";
}

<h1>Médicos en Cartillas</h1>

<p>
    @*<a asp-action="Create">Create New</a>*@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cartilla)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medico)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cartilla.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Medico.Especialidad)
                @Html.DisplayFor(modelItem => item.Medico.Nombre) 
            </td>
            <td>
               @* <a asp-action="Edit" asp-route-id="@item.idCartillaMedico">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idCartillaMedico">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idCartillaMedico">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
